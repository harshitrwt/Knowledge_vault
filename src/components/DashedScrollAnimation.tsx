"use client";

export default function Footer() {
  return (
    <footer className="border-t border-[var(--vault-line)] bg-[var(--vault-soft)] px-6 py-10 text-center text-sm font-bold text-[var(--vault-muted)]">
      Vault keeps document analysis organized from upload to answer.
    </footer>
  );
}
